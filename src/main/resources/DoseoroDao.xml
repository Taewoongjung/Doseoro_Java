<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myproject.doseoro.global.dao.DoseoroDao">

    <!--@@@@@@@@@@@@@@@@   identity     @@@@@@@@@@@@@@@@@-->
    <select id="findByEmail" resultType="AccessUser" parameterType="String">
        SELECT
            id AS id,
            email AS email,
            nick_name AS nickName
        FROM t_identity
        WHERE email = #{email}
    </select>

    <select id="existEmail" resultType="Boolean" parameterType="String">
        SELECT distinct * FROM t_identity WHERE email = #{email}
    </select>

    <select id="findUser" resultType="SignUpVO" parameterType="String">
        SELECT
            id AS id,
            email AS email,
            name AS name,
            nick_name AS nickName,
            phone AS phoneNumber,
            forgot_pw_question AS question,
            forgot_pw_answer AS answerForQuestion
        FROM t_identity
        WHERE email = #{email}
    </select>

    <!--    로그인     -->
    <select id="loginCheck" resultType="HashMap" parameterType="String">
        SELECT
            id AS id,
            email AS email,
            name AS name,
            nick_name AS nickName,
            phone AS phone,
            forgot_pw_question AS forgotPwQuestion,
            forgot_pw_answer AS forgotPwAnswer
        FROM t_identity
        WHERE email = #{email}
    </select>

    <!--    회원가입     -->
    <insert id="signUp" parameterType="SignUpVO">
        INSERT INTO t_identity(id, email, password, name, nick_name, phone, forgot_pw_question, forgot_pw_answer)
        VALUES(#{id}, #{email}, #{password}, #{name}, #{nickName}, #{phoneNumber}, #{question}, #{answerForQuestion})
    </insert>
    <!--@@@@@@@@@@@@@@@@   identity     @@@@@@@@@@@@@@@@@-->







    <!--@@@@@@@@@@@@@@@@   book     @@@@@@@@@@@@@@@@@-->

    <!--    책 등록    -->
    <insert id="registerBook" parameterType="RegisterBookDTO">
        INSERT INTO t_book
            (
                 id,
                 title,
                 price,
                 author,
                 publisher,
                 category,
                 state,
                 tradeMethod,
                 img,
                 about,
                 OwnerId
             )
        VALUES(
                   #{dto.id},
                   #{dto.title},
                   #{dto.price},
                   #{dto.author},
                   #{dto.publisher},
                   <foreach item="checkCategory" index="index" collection="dto.checkCategory" separator=" , ">
                        #{checkCategory},
                   </foreach>
                   #{dto.checkState},
                   #{dto.dealRoot},
                   <foreach item="images" index="index" collection="dto.images" separator=" , ">
                        #{images},
                   </foreach>
                   #{dto.about},
                   #{dto.ownerId}
               );
    </insert>

    <!--@@@@@@@@@@@@@@@@   book     @@@@@@@@@@@@@@@@@-->
</mapper>